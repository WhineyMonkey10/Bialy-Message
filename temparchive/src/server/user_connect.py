from src.database import *

userid = connect()

def connect_user():
    print(f"Connected. With user ID {userid}")
    # return to now wait for messages to be sent/received
    # execute code for that
